import React from 'react'
import ToggleButton from '../ToggleButton'

const Template: any = () => (
    <header className="header">
        <h1 className="header__title" title="reactjs">
            REACT<span>JS</span>
        </h1>

        <ToggleButton />
    </header>
)

export default Template
